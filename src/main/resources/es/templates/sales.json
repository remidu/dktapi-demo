{
    "script": {
        "lang": "mustache",
        "source": {
            "query": {
                "bool": {
                    "should": [
                        { "term": { "country": "{{country}}" } },
                        { "term": { "store": "{{store}}" } }
                    ]
                }
            }
        }
    }
}
